# Tax Engine Handoff — archived

**This file is stale.** Use **`docs/TAX-HANDOFF-2026-06-22.md`** (kept current as of 2026-06-24).

That doc reflects:

- `main` with planning attach **wired** in `index.html` after Scenarios Run
- **166** tests in `npm test` (engine + history + tax + planning)
- `node scripts/verify.mjs` visual gate (Household packet UI + full tab smoke)

Do not use branch/commit counts from this file for session planning.
